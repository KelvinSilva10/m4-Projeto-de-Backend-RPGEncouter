import AppDataSource from "../../data-source";
import { User } from "../../entities/user.entity";
import { AppError } from "../../errors/AppError";
import { ICampaignResponse } from "../../interfaces/campaign";
import { IUser, IUserResponse } from "../../interfaces/users";

const listCampaignsCreateByUserService = async (): Promise<ICampaignResponse[]> => {
  //CODIGO AQUI

  return 
};

export default listCampaignsCreateByUserService;
